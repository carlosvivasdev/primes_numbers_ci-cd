import { Routes } from '@angular/router';
import { PrimeCheckerComponent } from './prime-checker/prime-checker.component';

export const appRoutes: Routes = [
  { path: '', component: PrimeCheckerComponent } // Ruta por defecto
];